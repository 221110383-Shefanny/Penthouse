plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.flutter_application_9"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = "25.1.8937393"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.flutter_application_9"
        minSdkVersion 31
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    // Konfigurasi signing untuk release build
    signingConfigs {
        release {
            // Menggunakan path langsung ke keystore
            storeFile file("C:/Users/Shefanny/OneDrive/Documents/SEM5/penthouse/android/app/my-release-key.jks") // Path ke keystore Anda
            storePassword "blast123" // Gantilah dengan password keystore Anda
            keyAlias "key" // Gantilah dengan alias kunci Anda
            keyPassword "blast123" // Gantilah dengan password kunci Anda
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release // Menambahkan konfigurasi signing untuk release
            minifyEnabled true // Untuk optimasi kode dengan Proguard
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}
